$(function() {
	
	// Hide modal,	
	$('#sendMailModal').modal('hide');	
	
	// Clean the contents of inputs.
	$('#subject-text').val('');
	$('#message-text').val('');

	// Set notification message.
	$.notify('<%= @notify_message %>', '<%= @notify_flag %>');
});